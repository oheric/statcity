<!DOCTYPE html>
<html>
<head>
  <title></title>
<meta charset="utf-8">
<<<<<<< HEAD
<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">

<meta name="description" content="">
  <meta name="author" content="">

  <meta name="viewport" content="width=device-width,initial-scale=1">


  <link rel="stylesheet" src="../../assets/stylesheets/reset.css">
  <link rel="stylesheet" src="app/assets/stylesheets/bootstrap.css">
  <link rel="stylesheet" src="app/assets/stylesheets/style.css">

  <script src="app/assets/javascripts/libs/modernizr-2.0.6.min.js"></script>

<script src="http://d3js.org/d3.v3.min.js"></script>


</head>

<body>

<style>



h1{
	color: black;
}

button{
	float: right;
	margin-right: 300px;
}

</style>


<% @teams.each do |t| %>
<% t.players.each do |p| %>

<div><%= link_to p.name, playerlog_path(playerid: p.yahooid) %>

<!-- url_for("http://sports.yahoo.com/nba/players/" + p.yahooid.to_s + "/")  -->
</div>
<%end%>

<% end %>

<h1>Player Search</h1>

<%= form_tag playerlog_path, :method => 'get' do %>
    <%= text_field_tag :playerid %>
    <%= submit_tag "Search", :name => nil %>
<% end %>

<!-- D3 starts here -->

  <div id="container" class="container">
    <header>
    <h1>Gates Foundation Educational Spending</h1>
    </header>
    <div id="main" role="main">
      <div id="view_selection" class="btn-group">
        <a href="#" id="all" class="btn active">All Grants</a>
        <a href="#" id="year" class="btn">Grants By Year</a>
      </div>
      <div id="vis"></div>
    </div>
    <footer>
    <p>A demonstration of animated bubble charts in D3.js</p>
    <p><a href="http://vallandingham.me/bubble_charts_in_d3.html">Blog Post</a> | <a href="https://github.com/vlandham/gates_bubbles">Code</a> | <a href="http://www.washingtonpost.com/wp-srv/metro/data/gates-grants.html">Data</a></p>
    </footer>
  </div> <!--! end of #container -->


  <!-- <script src="//ajax.googleapis.com/ajax/libs/jquery/1.6.2/jquery.min.js"></script> -->
  <script>window.jQuery || document.write('<script src=app/assets/javascripts/libs/jquery-1.11.1.min.js"><\/script>')</script>

  <script defer src="app/javascripts/plugins.js"></script>
  <script defer src="app/javascripts/script.js"></script>
  <script src="app/assets/javascripts/CustomTooltip.js"></script>
  <script src="http://d3js.org/d3.v3.min.js"></script>
  <script src="app/assets/javascripts/vis.js"></script>
  <script type="text/javascript">
    $(document).ready(function() {
        $(document).ready(function() {
          $('#view_selection a').click(function() {
            var view_type = $(this).attr('id');
            $('#view_selection a').removeClass('active');
            $(this).toggleClass('active');
            toggle_view(view_type);
            return false;
          });
=======

<body>
<script src="http://d3js.org/d3.v3.min.js"></script>
<h1>StatCity</h1>
<script>

var margin = 20,
    diameter = 960;

var color = d3.scale.linear()
    .domain([-7, 4])
    .range(["hsl(152,80%,80%)", "hsl(228,30%,40%)"])
    .interpolate(d3.interpolateHcl);

var pack = d3.layout.pack()
    .padding(2)
    .size([diameter - margin, diameter - margin])
    .value(function(d) { return d.size; })

var svg = d3.select("body").append("svg")
    .attr("width", diameter)
    .attr("height", diameter)
  .append("g")
    .attr("transform", "translate(" + diameter / 2 + "," + diameter / 2 + ")");

d3.json("/gamelogs/load", function(error, root) {
  if (error) return console.error(error);

  var focus = root,
      nodes = pack.nodes(root),
      view;

  var circle = svg.selectAll("circle")
      .data(nodes)
    .enter().append("circle")
      .attr("class", function(d) { return d.parent ? d.children ? "node" : "node node--leaf" : "node node--root"; })
      .style("fill", function(d) { return d.children ? color(d.depth) : null; })
      .on("click", function(d) { if (focus !== d) zoom(d), d3.event.stopPropagation(); });

  var text = svg.selectAll("text")
      .data(nodes)
    .enter().append("text")
      .attr("class", "label")
      .style("fill-opacity", function(d) { return d.parent === root ? 1 : 0; })
      .style("display", function(d) { return d.parent === root ? null : "none"; })
      .text(function(d) { return d.name; });

  var node = svg.selectAll("circle,text");

  d3.select("body")
      .style("background", color(-10))
      .on("click", function() { zoom(root); });

  zoomTo([root.x, root.y, root.r * 2 + margin]);

  function zoom(d) {
    var focus0 = focus; focus = d;

    var transition = d3.transition()
        .duration(d3.event.altKey ? 7500 : 750)
        .tween("zoom", function(d) {
          var i = d3.interpolateZoom(view, [focus.x, focus.y, focus.r * 2 + margin]);
          return function(t) { zoomTo(i(t)); };
>>>>>>> b9392ea227ed02c2b7d8f2f538ebc971fd2410c6
        });
    });
  </script>


  <script>
    window._gaq = [['_setAccount','UA-17355070-1'],['_trackPageview'],['_trackPageLoadTime']];
    Modernizr.load({
      load: ('https:' == location.protocol ? '//ssl' : '//www') + '.google-analytics.com/ga.js'
    });
  </script>





</body>
</html>


